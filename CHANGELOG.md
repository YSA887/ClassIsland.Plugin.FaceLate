# 更新日志

本文件记录 ClassIsland 人脸早读考勤插件的全部版本变更。
版本号与 GitHub Release 的 tag 对应（形如 `1.2.3.0`，**纯数字、不带 `v` 前缀**）。

> 想了解插件能做什么、怎么安装和使用，请看 [README.md](README.md)。

---

## v1.2.3（修掉「换台电脑就检不到人脸」）

**现象**：同一张照片、同一个插件版本，在一台电脑上导入正常，换到另一台（例如教室一体机）
就报「没有检测到足够大的人脸」、批量导入「新增 0 条」，而且**每一张都失败**。

**根因：模型路径里有中文字符。** 这个坑藏得极深：

- OpenCV 的 C++ 实现是用**窄字符（ANSI）**路径去开文件的，而
  `FaceDetectorYN.Create` 只会把路径原样递进去、也没有「读进内存再加载」的重载。
- 于是只要检测模型所在路径含非 ASCII 字符，它就抛
  `OpenCVException: Can't read ONNX file`。
- 而 ClassIsland 的插件配置目录在 `C:\Users\<用户名>\...` 下 —— **Windows 用户名是中文**
  （`C:\Users\张老师\`），或者把 ClassIsland 装在 `D:\教学软件\ClassIsland` 这类目录里，
  路径就带中文了。
- 这个异常随后被 `EnsureDetectorLocked` 的 `catch` 吞掉，`Analyze` 返回空列表，
  界面最终显示成「未检测到人脸」——**看起来像照片或模型的问题，完全联想不到「装在哪个目录」**。

实测证据（探针，同一份模型放两个目录）：

| 交给 OpenCV 的路径 | 结果 |
| --- | --- |
| `D:\...\ascii\yunet.onnx` | ✅ 创建成功，正常检出（score 0.9429） |
| `D:\...\模拟一体机\教学软件\...\yunet.onnx` | ❌ `OpenCVException: Can't read ONNX file`（报错里路径已是乱码） |

**修复**：
1. 新增 `AsciiModelPath`：模型路径含非 ASCII 字符时，把检测模型（约 230 KB）
   复制到一个**纯 ASCII 目录**（优先 `C:\ProgramData\ClassIsland-FaceLate-Models\`，
   依次回退到系统临时目录、`Windows\Temp`）再交给 OpenCV；路径本来就是 ASCII 时不复制、零开销。
2. 识别模型也补了保险：按路径读失败时自动改用「读进内存再加载」（`CvDnn` 有字节数组重载）。
3. **不再吞错**：`FaceEngine` 新增 `LastAnalyzeError`，把「模型没载入 / 检测器创建失败及其原因」
   一路传到界面。以后遇到这类问题，提示会直接说出病因（例如「检测器初始化失败：……路径含非 ASCII 字符」），
   而不是笼统的「没有检测到足够大的人脸」；这类环境故障还会在「运行日志」里记一条错误。
4. 模型的「识别与模型」页新增一次**真实自检**：不只检查文件在不在，而是**真建一次检测器、真跑一次推理**，
   把「文件都在、但就是跑不起来」这种状态当场暴露出来。自检结果也会写进运行日志。

> 为什么以前没发现：开发机装在 `D:\Desktop\ClassIsland_...`、用户名也是纯数字，路径全 ASCII，
> 所以怎么测都正常。这类「只在别人机器上出现」的问题，只能靠**不吞异常 + 真实自检**来兜住。

---

## v1.2.2（界面重做：卡片排版 + 文案精简）

这一版把设置界面整个重排了一遍，并修掉两个会直接影响观感的 bug。

**修掉「设置项只剩一个光秃秃的开关」**
- 卡片的容器控件原本是带 XAML 内容的 `UserControl`，而 `UserControl` 的「自定义内容」
  就是 `Content` —— 调用方写在标签里的开关把卡片自己那棵视觉树（外框、图标、标题）整个覆盖掉了。
  表现是：卡片框、图标、标题、副标题全体消失，只剩下右侧孤零零一个控件，页面看着一大片空。
- 改成 `ContentControl` + `ControlTheme`（模板化控件）：模板负责画外观，`Content` 专门留给调用方。

**修掉「模型下拉框是空的」**
- `SelectedItem` 是双向绑定，它在 `InitializeComponent()` 时就取过一次值，
  而选项列表是构造函数后面才填的，于是一次性读到 `null`；之后往集合里 `Add`
  只会刷新下拉列表，不会让 `SelectedItem` 重读。现在建完选项后显式回填一次。

**修掉「切页签时两页叠在一起」**
- 同组单选按钮切换时，新按钮的 `Checked` 可能先于旧按钮被取消勾选触发，
  那一刻旧按钮还是 `IsChecked == true`，于是上一页留在画面上。
  改成按事件发送者判定，与触发顺序无关。

**排版**
- 设置页改为通宽卡片（原来限宽 760 且左对齐，右侧留一大片空）。
- 顶边栏右边常驻一行状态（模型就绪、已录入人数），原来堆在正文里的两条灰色信息条合并掉了。
- 「下次执行」挪到「时间计划」页 —— 它本来就属于那里。
- 加了分组小标题（考勤开关 / 判定逻辑 / 设备 / 连拍 / 自检 …），一组两三行。
- 按钮位置统一：卡片里的控件一律靠右；「批量导入」这类页面的两个选择按钮并排靠右；
  确认面板里「中止」单独放到最右边，避免误点。
- 每页内容都变短了，一周计划表的时间输入框限宽、`+ 当前 / 清空` 靠右对齐。

**文案**
- 所有副标题压到一行以内，去掉解释性长句。例如
  「关掉后到点不会自动抓拍，但手动『立即试用』仍然可用」→「关掉后不会自动抓拍」。
- 阈值提示原来是「当前模型：SFace（自带，稳妥），推荐值 0.363（正在用推荐值）。
  OpenCV FaceRecognizerSF 官方推荐的余弦阈值（对应 NORM_L2 阈值 1.128）。阈值不能跨模型照搬：
  不同模型的相似度分布不一样。」→ 现在是一行。
- 时间计划的描述会把连续且时间相同的日子并起来，七天相同就写「每天 07:20:00」，
  不再把一周七天各念一遍。

---

## v1.2.1（修正阈值：0.363 是 SFace 的，不是 ArcFace 的）

**问题**：v1.2.0 及以前的比对阈值是一个「全局」数字，默认 **0.363**。
但 0.363 是 **SFace** 的官方推荐值（OpenCV `FaceRecognizerSF`，对应 NORM_L2 阈值 1.128），
**ArcFace 系不是这个数**。余弦相似度的分布随模型结构、训练集、损失函数而变，
把 A 模型的阈值套到 B 模型上没有任何依据——InsightFace 官方指南里也明确写着
「never carry over a threshold across model versions」。

**修复**：阈值改成**跟着模型走**。

| | SFace（OpenCV Zoo） | ArcFace / MobileFaceNet（InsightFace） |
| --- | --- | --- |
| 推荐阈值 | **0.363** | **0.50** |
| 出处 | OpenCV 官方推荐（NORM_L2 对应 1.128） | InsightFace 官方指南 1:N 示例以 0.40 起步、社区实践常用 0.60，取中间值作起点 |
| 特征维度 | 128 | 512 |

改动细节：

1. **每款模型自带推荐阈值**（模型清单里的 `RecommendedThreshold`），切换到哪款就用哪款的推荐值 —— 不再有一个「通用 0.363」。
2. **阈值按模型分别保存**：你在 SFace 下调的 0.45、在 ArcFace 下调的 0.62 各记各的，
   切换模型不会把上一个模型的值带过去（这既是正确行为，也不会丢你自己的调参）。
3. **判定时取的是「实际载入的模型」**：设置里选的模型文件不存在而回退到 SFace 时，
   阈值也跟着回到 0.363，而不是继续用你以为在用的那款的值。
4. 设置页的阈值那一行下方会写明 **「当前模型是哪个、推荐值多少、你现在设的是多少」**，
   并新增「**恢复推荐值**」按钮。
5. 老配置自动迁移：旧的那个全局 `MatchThreshold` 如果被改过，会归到当时那款模型名下，不会丢。

> 顺带说明：阈值高低是「严格 ↔ 宽松」的取舍。调高更严格（少串脸、易漏认），
> 调低更宽松（易串脸、少漏认）。插件里另有「最少命中张数」靠多帧投票压制偶发误判，
> 所以拿不准时优先用推荐值，先动「最少命中张数」和「最小人脸像素」。

---

## v1.2.0（漏检根因修复 + 多脸框选 + 多模型清单，**需要 ClassIsland 2.1.0+**）

这一版修掉了一个**一直藏在底层的致命 bug**：人脸检测对任何非特定尺寸的图片都会失败，
而失败被上层 `catch` 转成了「未检测到人脸」，于是**竖版照片、手机拍的证件照、摄像头的俯拍画面统统报未检测到人脸**。

| # | 改进 | 说明 |
| --- | --- | --- |
| 1 | **修复「未检测到人脸」（根因）** | `FaceDetectorYN.Detect()` **不会自动缩放**输入——它要求输入尺寸**严格等于**检测器创建时的尺寸，否则直接抛 `Size does not match` 异常。之前检测器固定按某个尺寸建，喂任何别的尺寸的图都会失败（异常被吞掉→报「未检测到人脸」）。现在 `Analyze()` 内部先把图**等比例**缩放到工作尺寸（长边贴合 640×480 上限，**不补黑边、不拉伸**）再检测，检出后把框和 5 个关键点一起乘回原图坐标。<br>实测：竖版 1440×1920 → 检出 9 张脸；1440×1920 原本直接抛异常 |
| 2 | **多脸时手动框选录入** | 「从照片录入」和「从摄像头录入」遇到多张脸时，会弹出一个**带编号方框的预览图**，每张脸还单独列一个缩略图（摄像头录入还会标出「最像谁」），点一下就用你选的那张。以前是硬取最大的一张，一堆人合影里根本选不到自己想录的那个人 |
| 3 | **多款识别模型 + 自动挑选** | 识别模型从写死的枚举改成**清单**（`FaceModelCatalog`）。设置页多了「识别模型」下拉框：**自动（推荐）** / SFace（128 维，随包自带） / ArcFace MobileFaceNet（512 维，可选下载），每项标「已就绪 / 未下载」。选「自动」时优先挑磁盘上已有的、维度更高的那款；你手动选的那款没找到也会自动回退到自带的 SFace 并明确告诉你 |
| 4 | **一次备齐全部模型** | 模型操作菜单新增「**下载全部识别模型…**」，按体积从小到大逐个下载、已有的自动跳过，结束给出成功/已存在/失败的汇总。装完一次，以后换模型不用再等 |
| 5 | **设置页改成「一行一事」卡片 + 顶边栏页签** | 主设置页原来把启用开关、判定方式、7 行时间表、分辨率、抓拍张数全挤在一屏。现在改成**顶边栏 4 个页签**（基本 / 时间计划 / 摄像头 / 存档与日志），每页只放 3~5 行；每一行都是统一的卡片（**左侧图标 + 标题 + 一行灰色小字副标题 + 右侧控件**），设置项不再互相挤压。识别与模型页同样卡片化 |

> ⚠️ **升级提示**：分组菜单与自动化行动依赖 ClassIsland **2.1.0+** 的插件 API（`apiVersion: 2.1.0.0`）。宿主低于 2.1.0 请先升级 ClassIsland。
>
> 旧版本配置会自动迁移到新的「按星期」时间表与新的「识别模型」字段，无需重新填写。

**关于本次漏检修复的实测数据**（探针，同一批素材）：

| 场景 | 检出张数 | 最大脸宽 |
| --- | --- | --- |
| 横版 1280×720 | 5 张 | 89 px |
| 竖版 1440×1920 | 9 张 | 188 px |
| 竖版 1080×1920 | 7 张 | 179 px |
| 方形 960×960 | 6 张 | 123 px |
| 原图 2048×1150 | 12 张 | 141 px |

跨缩放特征稳定性：同一张脸在 1280×720 与 1920×1080 下的余弦相似度**最低 0.9879 / 中位 0.9926**（阈值 0.363），说明缩放不会毁掉特征。
关键点几何校验：完整可见的 5 张脸，眼-鼻-嘴的上下关系 5 张全部正确。

---

## v1.1.0（界面重构 + 录入效率 + 自动化，**需要 ClassIsland 2.1.0+**）

这一版把设置页从「一页铺满」改成了分组菜单，并补上了日常使用中最缺的批量录入能力。

| # | 改进 | 说明 |
| --- | --- | --- |
| 1 | **设置页分组菜单** | 原来所有设置挤在一层，找一项要滚半天。现在拆成 **5 个页面**挂在「人脸考勤」分组下：主设置（时间计划）、识别与模型、运行与日志、批量导入、花名册 |
| 2 | **日志精简** | 新增分级日志面板（细节 / 信息 / 成功 / 警告 / 错误），**默认折叠、细节行默认隐藏**，标题栏直接显示错误条数；带「复制」「清空」按钮 |
| 3 | **按钮下拉整合 + 防卡死** | 花名册页的多个按钮收进一个下拉菜单；执行期间相关按钮自动禁用（忙碌状态绑定），避免重复点击把界面点卡 |
| 4 | **批量导入（按文件名自动匹配）** | 照片文件名里带花名册人名时（如 `806班A.jpg`、`A-正面.png`）直接自动匹配到对应同学，一次可导入整批，结束后给出成功 / 跳过 / 失败的汇总 |
| 5 | **智能导入（逐张确认）** | 人像库里已有样本时，导入照片会**直接问「这是 XXX 吗」**，在设置页内嵌面板里逐张确认，不用来回弹窗 |
| 6 | **接入自动化「行动」** | 插件向自动化规则集提供行动 **「人脸考勤：执行一次考勤」**，可以直接拖进规则里。晚自习考勤这种非固定时间的场景，用自动化规则触发即可，不必再改插件配置 |
| 7 | **按星期分别设置时间** | 早读时间和周末托管的上学时间不一样，现在**周一到周日每天可单独开关、单独配时间，每天还能配多个时刻**（用逗号分隔） |

> ⚠️ **升级提示**：分组菜单与自动化行动依赖 ClassIsland **2.1.0+** 的插件 API（`apiVersion: 2.1.0.0`）。宿主低于 2.1.0 请先升级 ClassIsland。
>
> 旧版本配置会自动迁移到新的「按星期」时间表，无需重新填写。

**关于组件数量**：本版仍保留两个组件（主界面「早读迟到名单」与自动化用的迟到信息源）。它们在 ClassIsland 里是两种不同的绑定位，各自独立注册更稳定；本版新增的**分组菜单**已经解决了「设置项铺一层太乱」的问题。

---

## v1.0.1（重要修复版，建议升级）

修掉了 v1.0.0 上一批实际使用中暴露的问题：

| # | 现象 | 根因 | 修复 |
| --- | --- | --- | --- |
| 1 | 点「立即识别一次」报 `InvalidOperationException: Call from invalid thread`，**插件被 ClassIsland 自动禁用** | 识别跑在后台线程，却直接改了 Avalonia 控件的属性 | 新增 `UiThread` 帮助类；`AttendanceService` 的 `PropertyChanged`/`ResultChanged` 一律切回 UI 线程；设置页刷新状态前先判断线程 |
| 2 | 本地明明有模型却还在联网下载；学校网下不下来 → 模型永远载入失败 | 用 `AppContext.BaseDirectory` 找模型，而它在插件里指向的是 **ClassIsland 主程序目录**而非插件目录 | 改用 `Assembly.Location` 定位插件目录；新增「导入模型文件…」按钮；默认关闭联网下载 |
| 3 | 进度条不显示 | 界面里根本没有进度条，只有一行文字 | 新增 `ProgressPercent`/`StageText`/`ProgressVisible`，设置页加入真正的进度条，分阶段上报（载入模型 5% → 抓拍 22~50% → 比对 50~90% → 汇总 95%） |
| 4 | 后排同学认不准 | 默认抓拍只有 **640×480**，后排人脸只有二三十像素，放大到 112×112 后特征全糊 | 默认分辨率提到 **1280×720**，检测置信度阈值 0.7→0.6，新增可调的「最小人脸像素」（默认 60） |
| 5 | 无法录入人脸，提示「模型无法加载」 | 同 #2 | 同 #2；花名册页一打开就把模型状态和目录写进日志 |
| 6 | 抓拍时间只能精确到分钟 | 解析器只接受 `HH:mm`，定时器 10 秒轮询一次 | 支持 `HH:mm:ss`，轮询改为 1 秒，并加了「填入当前时间」按钮方便调试 |
| 7 | 640×480 到底够不够准 | —— | 实测证明**不够**。见 [README 第七节](README.md#七耗时与性能) 的对比数据 |

---

## v1.0.0

首个版本：定时抓拍、人脸录入、缺席/出现两种判定、主界面迟到名单组件。
